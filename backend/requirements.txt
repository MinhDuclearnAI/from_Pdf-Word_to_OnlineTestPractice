# ==========================================
# 1. Core Framework & Server
# ==========================================
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
# Bắt buộc phải có để FastAPI xử lý được UploadFile và Form(...) trong file exam_upload.py
python-multipart>=0.0.9

# ==========================================
# 2. Data Validation & Configuration
# ==========================================
pydantic>=2.6.0
pydantic-settings>=2.2.0
# Bắt buộc phải có để Pydantic hỗ trợ kiểm tra định dạng EmailStr trong schema User
email-validator>=2.1.0

# ==========================================
# 3. Database & ORM
# ==========================================
# Sử dụng SQLAlchemy 2.0+ với kiến trúc mới
SQLAlchemy>=2.0.29
alembic>=1.13.1
# Driver kết nối PostgreSQL (dùng bản binary để không phải cài thêm trình biên dịch C trên Docker)
psycopg2-binary>=2.9.9

# ==========================================
# 4. Security & Authentication
# ==========================================
# Thư viện chuẩn để tạo và giải mã JWT Token
PyJWT>=2.8.0
# Thư viện dùng để băm (hash) và xác thực mật khẩu
passlib[bcrypt]>=1.7.4
bcrypt>=4.1.2

# ==========================================
# 5. Background Tasks & Caching
# ==========================================
celery>=5.3.6
# Redis client hỗ trợ cả kết nối đồng bộ (cho Celery) và bất đồng bộ (cho WebSocket/FastAPI)
redis>=5.0.3

# ==========================================
# 6. AI & LLM SDKs
# ==========================================
# SDK chính thức để tương tác với các mô hình ngôn ngữ lớn
openai>=1.14.0
anthropic>=0.21.3
google-generativeai>=0.4.1

# ==========================================
# 7. File Processing & Cloud Storage
# ==========================================
# SDK của AWS, dùng để tương tác với S3 hoặc MinIO
boto3>=1.34.60
# PyMuPDF (fitz) - Thư viện bóc tách text từ PDF tốc độ cao và giữ format tốt nhất hiện nay
PyMuPDF>=1.24.0
# Đọc và bóc tách dữ liệu từ file Word (.docx)
python-docx>=1.1.0

# ==========================================
# 8. Utilities
# ==========================================
# HTTP client dùng để gọi API ra bên ngoài (hoặc dùng trong Test)
httpx>=0.27.0
# Hỗ trợ parse timezone chính xác
pytz>=2024.1